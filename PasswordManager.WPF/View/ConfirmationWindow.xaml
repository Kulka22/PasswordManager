<Window x:Class="PasswordManager.WPF.ConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.WPF"
        mc:Ignorable="d"
        Title="Подтверждение"
        SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <TextBox Grid.Row="0"  Text="{Binding Message}" Margin="5" VerticalAlignment="Top" HorizontalAlignment="Center" />
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5,0,5,5">
            <Button Content="Да" Height="30" Width="50" Margin="0,0,5,0" VerticalAlignment="Top" HorizontalAlignment="Left" Click="ButtonOkClick" />
            <Button Content="Нет" Height="30" Width="50" Margin="5,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Click="ButtonNegativeClick" />
        </StackPanel>
    </Grid>
</Window>
